<%# A single figure with a caption beneath — their .metric. %>
<div class="rounded-2xl border border-rule bg-white p-5 text-center">
  <p class="font-display text-3xl text-village"><%= value %></p>
  <p class="mt-1 text-sm text-muted"><%= caption %></p>
</div>
