<%# Their .privacy block — a bordered statement, used where the page makes a
    plain-language commitment rather than describing a feature. %>
<div class="mt-6 rounded-xl border-l-4 border-village bg-white p-5">
  <p class="font-display font-bold text-village"><%= heading %></p>
  <div class="mt-2 text-sm text-muted"><%= yield %></div>
</div>
