development:
  adapter: async

test:
  adapter: test

production:
  # No Redis in this deployment, and the app defines no Action Cable channels.
  # Revisit if real-time features are added.
  adapter: async
